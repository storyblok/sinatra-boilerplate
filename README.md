# Storyblok Sinatra Boilerplate

Example to show the integration of Storyblok in Sinatra.

# How to run

```
bundle install
rerun --pattern '{Gemfile,Gemfile.lock,app.rb,app/**/*.rb}' 'ruby app.rb'
```
